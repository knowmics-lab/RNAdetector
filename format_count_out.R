#### Change HTseq-count and FeatureCounts output files in the same format ####
path <- "D:/NMSpipeline/read_counts/"
counts_files_names <- list.files(path, pattern = "\\.txt")
for (counts in counts_files_names){
  if ()
}